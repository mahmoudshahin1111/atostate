{
  "compilerOptions": {
    /* Target & Environment */
    "target": "es2024",                // High enough for Node 18+, compatible with modern browsers
    "lib": ["ESNext", "DOM", "DOM.Iterable"], // Includes both JS and Browser APIs (window, document)
    "types": ["node"],                 // Explicitly include Node types (process, __dirname)

    /* Module System */
    "module": "ESNext",                // Standard for both modern Node and Bundlers
    "moduleResolution": "bundler",     // Supports package.json exports while remaining flexible
    "resolveJsonModule": true,
    "isolatedModules": true,           // Required for most web bundlers and transpilers

    /* Interop & Strictness */
    "esModuleInterop": true,           // Compatibility with CommonJS modules
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true               // Speeds up build by skipping type checks in node_modules
  },
  "include": ["src/**/*"]
}